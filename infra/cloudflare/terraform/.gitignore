# Local Terraform state, plugins, and crash logs
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Real variable files — only the *.example files are committed.
*.tfvars
!*.tfvars.example

# Plan output
*.tfplan
