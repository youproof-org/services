{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://youproof.org/schemas/migration-manifest.json",
  "title": "Migration manifest",
  "description": "Static slug map bundled into the .hu -> .org migration Worker. Keys are legacy paths, values are the new .org paths to 301-redirect to.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "updatedAt", "entries"],
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "updatedAt": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO date (YYYY-MM-DD) the manifest was last updated."
    },
    "entries": {
      "type": "object",
      "description": "Map of old legacy path -> new .org path.",
      "propertyNames": {
        "pattern": "^/(|.*[^/])$",
        "description": "Leading slash, no trailing slash (except the root '/')."
      },
      "additionalProperties": {
        "type": "string",
        "pattern": "^/(|.*[^/])$",
        "description": "Leading slash, no trailing slash (except the root '/')."
      }
    }
  }
}
